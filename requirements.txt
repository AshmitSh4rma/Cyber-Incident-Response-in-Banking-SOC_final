# Web layer
fastapi>=0.100.0
uvicorn>=0.22.0
python-multipart>=0.0.6

# Optional: local LLM enrichment for Layer 4. The pipeline runs fully without it.
langchain-ollama>=0.1.0
requests>=2.31.0

# Tests
pytest>=7.0.0
