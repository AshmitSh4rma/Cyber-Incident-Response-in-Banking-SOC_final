.venv/
venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
*.db
*.sqlite
*.sqlite3
node_modules/
.next/
dist/
build/
.cache/
*.log
.DS_Store

# Pipeline artifacts. frontend_output.json and soc_incidents.db are committed on
# purpose so a fresh clone has demo data; per-layer dumps are debug output.
layer1_output.json
layer2_output.json
layer3_output.json

# Runtime configuration. Per-deployment state, not source: a fresh clone runs on
# the shipped defaults in soc_config.py until someone changes something.
soc_config.json
soc_config_audit.json
